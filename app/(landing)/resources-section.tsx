import { Container } from '@/components/ui/container';
import { SectionLabel } from './section-label';

export const ResourceSection = () => {
  return (
    <Container>
      <SectionLabel
        label="how it works?"
        headline="Everything u need to know for using Pluto"
      />
    </Container>
  );
};
